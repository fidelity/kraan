
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: layermgrs.kraan.io
spec:
  group: kraan.io
  names:
    kind: LayerMgr
    listKind: LayerMgrList
    plural: layermgrs
    singular: layermgr
  scope: Cluster
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: LayerMgr is the Schema for the layer manager API.
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: LayerMgrSpec defines the desired state of LayerMgr.
          properties:
            generation:
              description: Generation is used to distinguish between different updates
              format: int64
              type: integer
            hold:
              description: This flag tells the controller to hold off deployment of
                all addons,
              type: boolean
            phase:
              description: The phase of addons layer processing to execute
              type: string
          required:
          - phase
          type: object
        status:
          description: LayerMgrStatus defines the observed status.
          properties:
            conditions:
              description: Conditions history.
              items:
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime is the timestamp corresponding
                      to the last status change of this condition.
                    format: date-time
                    type: string
                  message:
                    description: Message is a human readable description of the details
                      of the last transition, complementing reason.
                    type: string
                  reason:
                    description: Reason is a brief machine readable explanation for
                      the condition's last transition.
                    type: string
                  status:
                    description: Status of the condition, one of ('True', 'False',
                      'Unknown').
                    type: string
                  type:
                    description: Type of the condition.
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            state:
              description: State is the current state of the layer manager.
              type: string
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
